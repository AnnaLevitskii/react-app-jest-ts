pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'build'
            }
        }
        stage('testing') {
            steps {
                echo 'testing'
            }
        }
    }
}
