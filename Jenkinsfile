pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'build'
            }
        }
    }
  stages {
        stage('testing') {
            steps {
                echo 'testing'
            }
        }
    }
}
